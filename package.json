{
  "name": "grid-components",
  "version": "0.1.1",
  "description": "Dead simple grid for react using flexbox spec",
  "main": "dist/index.ts",
  "types": "dist/index.d.ts",
  "scripts": {
    "prepublishOnly": "npm run build",
    "build": "tsc",
    "test": "ech \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "grid",
    "react",
    "flex",
    "flexbox"
  ],
  "author": "Bryan Willem",
  "license": "MIT",
  "dependencies": {
    "react": "^16.0.0",
    "styled-components": "^2.2.2"
  },
  "devDependencies": {
    "@types/react": "^16.0.18",
    "typescript": "^2.5.3"
  }
}
